FRONTEND_ORIGIN=*
PORT=3001
ENABLE_DEMO_DATA=false
READING_RETENTION_HOURS=48
# Store this value in Replit Secrets, not in source control.
DEVICE_API_KEY=replace-with-a-long-random-device-key
# Frontend and backend are served from the same origin in deployment.